{
  "name": "@userscript-proxy/backend",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --build tsconfig.json && esbuild --tsconfig=tsconfig.json --bundle src/index.ts --outdir=dist --platform=node --out-extension:.js=.cjs --minify",
    "dev": "tsx watch src/index.ts"
  },
  "dependencies": {
    "@fastify/cors": "^11.2.0",
    "@userscript-proxy/core": "*",
    "fastify": "^5.8.2"
  }
}
